<h1 class="text-center mt-4">Create a new Category</h1>
<div class="container">
  <%= render 'shared/errors', obj: @category %>
  <div class="row g-3 justify-content-center">
    <div class="col-10">
      <%= render 'form' %>
    </div>

    <%= link_to '[ Cancel and return to categories Listing ]', categories_path, class: "text-info mb-3" %>
  </div>
</div>
